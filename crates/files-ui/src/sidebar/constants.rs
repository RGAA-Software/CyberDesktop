//! Sidebar layout constants aligned with the main window title tab bar.

use gpui::px;

/// Sidebar row height (design V11).
pub const SIDEBAR_ITEM_HEIGHT: gpui::Pixels = px(35.);
pub const SIDEBAR_ITEM_RADIUS: gpui::Pixels = px(10.);
