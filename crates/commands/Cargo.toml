[package]
name = "cyber-desktop-commands"
description = "Command actions and key bindings for CyberDesktop"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
gpui.workspace = true
